<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tute.estacionamientofrlp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".AppController"
        android:allowBackup="true"
        android:icon="@mipmap/ic_efrlp"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".RegistrationActivity"
            android:theme="@style/AppTheme"></activity>

        <activity
            android:name=".DeshacerActivity"
            android:label="Deshacer compras"
            android:theme="@style/AppTheme"></activity>

        <activity
            android:name=".GetCompras"
            android:theme="@style/AppTheme"></activity>


        <activity
            android:name=".MainActivity"
            android:theme="@style/AppTheme"></activity>

        <activity
            android:name=".CompraActivity"
            android:label="Comprar días"
            android:theme="@style/AppTheme"></activity>

        <activity
            android:name=".HistorialesActivity"
            android:label="Historiales"
            android:theme="@style/AppTheme"></activity>

        <activity
            android:name=".GestCuentaActivity"
            android:label="Cambio de contraseña"
            android:theme="@style/AppTheme"></activity>

        <activity
            android:name=".GestPatesActivity"
            android:label="Gestionar patentes"
            android:theme="@style/AppTheme"></activity>


    </application>

</manifest>